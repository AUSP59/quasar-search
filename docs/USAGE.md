# Usage

Build the project as described in the README, then run:

```bash
./build/apps/quasar-grep "needle" /path --count --jobs 8
```

Use `--json` to print machine‑readable metrics, or `--no-color` to disable ANSI colors.