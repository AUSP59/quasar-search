# Benchmarking Guide
- Use `bench/` harness. Pin CPU frequency; isolate cores if possible.
- Run at least 10 iterations; report median and p95.
- Keep data and environment snapshots with the results artifact.
