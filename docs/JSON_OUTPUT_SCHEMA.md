# JSON Output Schema

`quasar-grep --json` emits a machine-readable summary. The canonical schema lives at `schemas/quasar_grep.schema.json`.
CI validates the output against the schema in `json-schema.yml`.
