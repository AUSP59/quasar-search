# ADR-0001: Enable optional mmap and SIMD prefilter
- **Status**: Accepted
- **Context**: Improve throughput on large files and long needles.
- **Decision**: Add `--mmap` and `--simd` flags with runtime detection and scalar verification.
- **Consequences**: Higher performance where supported; same results; portable fallback.