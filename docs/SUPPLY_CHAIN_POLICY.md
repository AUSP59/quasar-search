# Supply Chain Policy (Sigstore Policy Controller)

Use `policy/verification/policy.yaml` to require images from `ghcr.io/OWNER/REPO` to be signed with GitHub OIDC (cosign).
Adjust `identities` for your org. See https://docs.sigstore.dev/policy-controller/.
