# Performance & Size Budgets
- Binary size budget enforced by `size-guard.yml` (`MAX_SIZE_KB` repo variable).
- Global coverage minimum via `coverage-gate.yml` (`MIN_COVERAGE`).
- Patch coverage via `patch-coverage.yml` (`MIN_PATCH_COVERAGE`).
