# Branching Model
- Trunk-based development on `main` with short-lived feature branches.
- Release branches for `vMAJOR.MINOR.x` maintenance when needed.
- Protected `main` with required CI checks and code reviews.
