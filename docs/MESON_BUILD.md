# Meson Build (Skeleton)
A minimal `meson.build` is provided for adopters using Meson. Adjust the file lists to match your source tree.
```bash
meson setup builddir
meson compile -C builddir
meson install -C builddir
```
