name: gitleaks
true:
- push
- pull_request
jobs:
  scan:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: gitleaks/gitleaks-action@v2
'on':
- workflow_dispatch
