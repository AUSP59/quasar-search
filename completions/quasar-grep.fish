complete -c quasar-grep -l help -d "Show help"
complete -c quasar-grep -l count -d "Count matches"
complete -c quasar-grep -l json -d "Output JSON"
complete -c quasar-grep -l progress -d "Show progress"
complete -c quasar-grep -l max-bytes -d "Max bytes to read" -r
complete -c quasar-grep -l trace -d "Trace output"
